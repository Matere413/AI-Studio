"""Shared security helpers for the AI-Studio API.

- :mod:`cookies` — auth cookie set/clear helpers (centralizes attributes).
- :mod:`redaction` — structlog processor that scrubs secret keys from logs.
"""